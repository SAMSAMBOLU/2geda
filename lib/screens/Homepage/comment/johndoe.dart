import 'package:flutter/material.dart';

class johnDoe extends StatelessWidget {
  const johnDoe({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Comment"),
      ),
    );
  }
}