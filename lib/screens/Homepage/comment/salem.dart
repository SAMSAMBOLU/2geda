import 'package:flutter/material.dart';

class postSalem extends StatelessWidget {
  const postSalem({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Comment"),
      ),
    );
  }
}