import 'package:flutter/material.dart';

class postRichard extends StatelessWidget {
  const postRichard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Comment"),
      ),
    );
  }
}